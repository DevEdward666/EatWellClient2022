import {StyleSheet,Dimensions} from 'react-native'

const styles = StyleSheet.create({
    input: {
      height: 40,
      width:"100%",
      color:"black",
      textAlign:"center",
      borderWidth: 1,
    },
  });
  
  export default styles